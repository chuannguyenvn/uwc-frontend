.timeline-line {
    width: 2px;
    background-color: var(--grey-text-color);
    flex-grow: 1;
}

#TaskListEntry {
    flex-direction: row;
    align-items: stretch;
    margin-left: 24px;
    margin-right: 24px;
}

#TimelineContainer {
    align-items: center;
    min-width: 18%;
}

.task-card {
    flex-grow: 1;
    padding: 16px;
}

#ContentContainer {
    border-radius: 32px;
    padding: 16px;
}
