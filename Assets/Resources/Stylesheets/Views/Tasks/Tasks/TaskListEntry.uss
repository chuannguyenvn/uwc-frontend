.timeline-line {
    width: 2px;
    background-color: var(--grey-text-color);
    flex-grow: 1;
}

#TaskListEntry {
    flex-direction: row;
    align-items: stretch;
}

#TimelineContainer {
    align-items: center;
    min-width: 12%;
}

.task-card {
    flex-grow: 1;
    padding: 16px;
}

#ContentContainer {
    border-radius: 32px;
    padding: 16px;
}
