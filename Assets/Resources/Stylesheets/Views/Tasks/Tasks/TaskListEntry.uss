.timeline-line {
    width: 2px;
    background-color: rgb(0, 0, 0);
    flex-grow: 1;
}

#TaskListEntry {
    flex-direction: row;
    align-items: stretch;
}

#TimelineContainer {
    align-items: center;
}

.task-card {
    flex-grow: 1;
    padding: 16px;
}

#ContentContainer {
    overflow: hidden;
    border-radius: 32px;
    padding: 16px;
}
